#lang racket
(define (buildbst tree)
  (cond
    ((null? (cdr tree)))
    (buildbst (insert (cdr)))
  )
)
(define (insert tree value)
  (cond
    (< value (car tree) (cons (car tree) (list value '() ) ) )
  )
)
(define (root val)
  (list val '() '())
)